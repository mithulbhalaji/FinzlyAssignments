import { Component } from "@angular/core";

@Component({
    selector : 'app-employee', //Only one type script file
    templateUrl : './employee.component.html', //Every component should have only one html
    styleUrls : ['./employee.component.css'] //We can use multiple css
})

export class EmployeeComponent{
    
}